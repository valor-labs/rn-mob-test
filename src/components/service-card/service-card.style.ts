import { StyleSheet } from 'react-native';

export const serviceCardStyle = StyleSheet.create({
    container: {
        backgroundColor: '#ECF1F4',
        borderRadius: 5,
        flexDirection: 'row',
        alignItems: 'center',
        height: 65,
    },
    title:{ fontSize: 20 }
});
